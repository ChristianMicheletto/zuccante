/* define a class 
class GreetingTask extends DefaultTask {
}
*/

// extends a class
class GreetingTask extends DefaultTask {
    @TaskAction
    def greet() {
        println 'hello from GreetingTask'
    }
}

// define a task
task hello(type: GreetingTask)
